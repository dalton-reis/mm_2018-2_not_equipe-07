using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class quadScriptPro : MonoBehaviour {

    private GlobalClass global = GlobalClass.Instance();
    public string nameBtn = "";
    public 
    // Use this for initialization
    void Start () {
        
    }
	
	// Update is called once per frame
	void Update () {

       /* switch (this.nameBtn)
        {
            case "btnChar":
                GetComponent<TextMeshPro Text>().text = global.caracteristica;
            break;
            case "btnFunc":
                GetComponent<TextMeshPro>().text = global.funcoes;
            break;
            case "btnQuest":
                GetComponent<TextMeshPro>().text = global.perguntas;
            break;            
        }
        
    */

       // if (global.botaoAtivo.Equals(nameBtn))
        
		
	}
}
